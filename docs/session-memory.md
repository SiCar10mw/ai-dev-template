# Session Memory

This file is lightweight working memory for future sessions.

## Current State

- Template scaffold initialized.
- Mandatory methodology and default Python quality gates are in place.
- MCP governance, Docusaurus docs-site, documentation-impact gate, and document generation defaults are in place.
- Identity/secrets onboarding now requires `make setup-identity` before `make operationalize`.

## Next Session Prompts

- Review `BACKLOG.md`.
- Pick one backlog item only.
- Run `make check` before handoff.
- Keep docs current in the same change as code.
