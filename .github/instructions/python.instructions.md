---
applyTo: "**/*.py"
---

Use Python 3.11+ style, type hints for new functions, deterministic tests, and `ruff` formatting conventions. Keep
imports sorted and line length at 120 characters. Avoid shell string construction; when subprocesses are necessary, pass
argument arrays and validate inputs first.
