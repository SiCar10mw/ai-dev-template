{
  "default_client": "first-class-tool-router",
  "source_of_truth": "This file documents preferred model roles. The active client or enterprise policy decides availability.",
  "first_class_tools": [
    {
      "rank": 1,
      "profile": "anthropic_claude",
      "tool": "Anthropic Claude",
      "provider": "anthropic",
      "model_family": "claude",
      "model_id": "claude-enterprise-default"
    },
    {
      "rank": 2,
      "profile": "google_gemini",
      "tool": "Google Gemini",
      "provider": "google",
      "model_family": "gemini",
      "model_id": "gemini-enterprise-default"
    },
    {
      "rank": 3,
      "profile": "openai_codex_gpt",
      "tool": "OpenAI Codex/GPT",
      "provider": "openai",
      "model_family": "gpt-codex",
      "model_id": "gpt-codex-enterprise-default"
    }
  ],
  "fourth_view": {
    "profile": "github_copilot",
    "tool": "GitHub Copilot",
    "provider": "github",
    "model_family": "enterprise-routed",
    "model_id": "copilot-enterprise-default"
  },
  "rules": [
    "Do not commit provider API keys, bearer tokens, OAuth refresh tokens, or private endpoint URLs.",
    "Use repository configuration for model roles; do not rely on model memory for provider selection.",
    "Use deterministic tools for verdicts, gate results, approval state, and release status."
  ],
  "model_roles": {
    "primary_coding": {
      "preferred_order": ["anthropic_claude", "google_gemini", "openai_codex_gpt"],
      "purpose": "Implementation, refactoring, test generation, and local reasoning."
    },
    "independent_review": {
      "preferred_order": ["google_gemini", "anthropic_claude", "openai_codex_gpt"],
      "purpose": "Adversarial review with a different model family from the primary coding pass when available."
    },
    "security_review": {
      "model_profile": "anthropic_mythos_fable_5",
      "preferred_order": ["anthropic_mythos_fable_5", "anthropic_claude", "google_gemini", "openai_codex_gpt"],
      "purpose": "AI-SAST defensive finder. The model produces structured candidate findings only; scripts/check_ai_sast.py is the deterministic decider.",
      "structured_output": "json",
      "temperature": 0.0
    },
    "documentation": {
      "preferred_order": ["openai_codex_gpt", "anthropic_claude", "google_gemini"],
      "purpose": "Contributor docs, public docs-site copy, DOCX drafts, and deck drafts."
    }
  },
  "specialist_profiles": {
    "anthropic_mythos_fable_5": {
      "provider": "anthropic",
      "model_family": "claude-fable",
      "model_id": "claude-fable-5-mythos-20260601",
      "api_version": "2023-06-01",
      "pinned": true,
      "purpose": "defensive security review finder only"
    }
  },
  "provider_headers": {
    "openai_codex_gpt": {
      "api_key": "Set OPENAI_API_KEY or Azure OpenAI credentials in the environment or secret manager.",
      "notes": "Never store authorization headers in this file."
    },
    "anthropic_mythos_fable_5": {
      "api_key": "Set ANTHROPIC_API_KEY in the environment or secret manager.",
      "version_header": "Use the pinned provider API version in the runtime client; do not commit secrets.",
      "notes": "Public Fable 5 cyber policy gates offensive analysis. This route is for defensive code scanning only and may refuse offensive requests."
    },
    "anthropic_claude": {
      "api_key": "Set ANTHROPIC_API_KEY in the environment or secret manager.",
      "version_header": "Set the provider-required version header in the client configuration, not in committed secrets."
    },
    "google_gemini": {
      "api_key": "Set GEMINI_API_KEY or the enterprise-approved equivalent in the environment or secret manager.",
      "notes": "Use the enterprise-approved Gemini client or routing policy where available."
    }
  }
}
